api
express.js
middlewear



npm 
--npm installed
npm --version
--it will shows the version
node --version
--it will shows node version

npm install express
npm uninstall express

npm install slugify(if wants to download specific version then npm install slugify@(version address))
--normally used for make update and fix the bugs.(1.3.6)
  	-1 shows the major changes
	-3 shows the minor changes
	-6 shows the bug fixer

**nodemon
npm install nodemon --save-dev
npm install nodemon --global
--it will restart your server automatically
------in dependencies if browserify and slugify uses ^(cap)sign then it will download the version as user specified but id ~(tilled) sign is used then 
      then it will download the current version . as you want to download there person version then just type npm i
------ in devdependencies nodemon normally uses ^ sign but by using (>  greater than) it will downloads the current version 
	for view current versions you should write:-npm view (package name) version

